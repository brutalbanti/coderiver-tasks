import { SectionHeader } from '../Section/SectionHeader';
import { SectionSlider } from '../Section/SectionSlider';

export const Main = () => {
    return (
        <main className="page">
            <SectionHeader />
            <SectionSlider />
        </main>
    )
}